/////////////// TC4 //////////////////

/*

Prueban que match se transpila correctamente a Java y funciona correctamente en
conjunción con data y ^ .
  
*/


let foo = 1
fun a = x -> 666
data foo{ // Identificador redefinido `foo`
a, // Identificador redefinido `a`
foo // Identificador redefinido `foo`
}
// Note que no hay identificador `b` redefinido aquí
let b = x -> b
